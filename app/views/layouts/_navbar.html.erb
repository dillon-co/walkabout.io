<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <% if set_account %>
          <%= link_to set_account.name, portfolio_path, class: "navbar-brand" %>
        <% else %>
          <%= link_to 'WALKABOUT', marketing_path, class: "navbar-brand" %>
        <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if set_account %>
          <% set_page.each do |sp| %>
            <li><%= link_to sp.name, sp.name %></li>
          <% end %>
            <li><%= link_to 'Hire Me', contact_path %></li>
        <% else %>
          <li><%= link_to 'Pricing', "#" %></li>
          <li><%= link_to 'Blog', "#" %></li>
          <li><%= link_to 'Sign Up', "#" %></li>
        <% end %>
        <% if current_user && current_user.name == set_account.name %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-bars"></i></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Analytics', analytics_path %></li>
            <li><%= link_to 'Settings', edit_user_path(set_account.id) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Logout', sign_out_path %></li>
          </ul>
        </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>